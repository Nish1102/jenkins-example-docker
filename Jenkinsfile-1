pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Test') {
      steps {
        dir(env.WORKSPACE) {
          sh 'node --version'
          // Add more commands as needed
        }
      }
    }
  }
}
